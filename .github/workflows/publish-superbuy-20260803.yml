name: Publish Superbuy August 3 guide

on:
  push:
    branches:
      - main
    paths:
      - superbuyvip.shop/.publish/superbuy-2026-08-03.flag

permissions:
  contents: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Patch and validate Superbuy publication
        run: python .github/scripts/publish_superbuy_20260803.py

      - name: Commit production update
        run: |
          git diff --check
          git diff --quiet -- superbuyvip.shop/index.html && { echo "Expected homepage change was not produced"; exit 1; }
          git diff --quiet -- superbuyvip.shop/guides/index.html && { echo "Expected guide archive change was not produced"; exit 1; }
          git diff --quiet -- superbuyvip.shop/sitemap.xml && { echo "Expected sitemap change was not produced"; exit 1; }
          rm -f .github/scripts/publish_superbuy_20260803.py
          rm -f .github/workflows/publish-superbuy-20260803.yml
          rm -f superbuyvip.shop/.publish/superbuy-2026-08-03.flag
          git config user.name "seo-sites automation"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git commit -m "Feature August 3 Superbuy risk guide"
          git push origin HEAD:main
