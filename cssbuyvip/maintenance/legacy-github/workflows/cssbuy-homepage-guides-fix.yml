name: Legacy CSSBuy Homepage Guides Fix (Disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy fixed-article workflow disabled; daily guides are managed by cssbuy-sync-guides-all-langs.yml."
